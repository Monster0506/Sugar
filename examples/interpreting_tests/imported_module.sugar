DEF m #{#str, #int} = {"a"->5}
CLASS Adder
    value #int
    CONSTRUCTOR()
        DEF THIS.value #int = 0
    end
    FUNC add(x #int) #int
        THIS.value := THIS.value + x
        RETURN THIS.value
    end
end
DEF a #Adder = Adder()
a:add:(1)
