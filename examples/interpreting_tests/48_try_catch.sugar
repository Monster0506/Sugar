
TYPE MyError EXTENDS Error
    message #str
end

FUNC risky() #void
    THROW MyError("fail!")
end


DEF y #str = ""
FUNC test() #str
    TRY
        risky()
        RETURN "ok"
    CATCH e #MyError do
        RETURN e.message
    FINALLY do
        y := "done"
    end
end 


DEF x #str = test()
