// Pattern Matching Example

FUNC describe_value(value #any) #str
    MATCH value
        CASE 0 do
            RETURN "zero"
        CASE #int n if $n < 0$ do
            RETURN "negative number"
        CASE #int n do
            RETURN "positive number"
        CASE #str s do
            RETURN "string: " + s
        CASE #[#int] arr if $arr :LENGTH:() > 3$ do
            RETURN "long array"
        CASE #[#int] arr do
            RETURN "short array"
        DEFAULT do
            RETURN "unknown type"
    end
end

FUNC process_command(command #any) #str
    MATCH command
        CASE "start" do
            RETURN "Starting application..."
        CASE "stop" do
            RETURN "Stopping application..."
        CASE "restart" do
            RETURN "Restarting application..."
        CASE #str cmd if $cmd :LENGTH:() > 10$ do
            RETURN "Long command: " + cmd
        DEFAULT do
            RETURN "Unknown command"
    end
end

// Usage
DEF result1 #str = describe_value(0)
DEF result2 #str = describe_value(-5)
DEF result3 #str = describe_value("hello")
DEF result4 #str = describe_value([1, 2, 3, 4, 5])
DEF result5 #str = process_command("start")

// Print results
IO:PRINT:("Result 1: " + result1)
IO:PRINT:("Result 2: " + result2)
IO:PRINT:("Result 3: " + result3)
IO:PRINT:("Result 4: " + result4)
IO:PRINT:("Result 5: " + result5) 
